@css {
  @tailwind base;
  @tailwind components;
  @tailwind utilities;
}

body
  @apply m-0 p-0

.btn
  @apply px-4 py-2 rounded cursor-pointer

  &.primary
    @apply bg-blue-600 text-white

  &.success
    @apply bg-green-600 text-white
